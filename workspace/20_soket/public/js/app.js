//클라이언트 접속방법
// const soket = new WebSocket(`ws://localhost:6060`);
// const soket = new WebSocket(`ws://${window.location.host}`);

// const form = document.querySelector("form");

// function sendMessage(event) {
//   event.preventDefault();
//   const input = document.querySelector("input");
//   soket.send(input.value);
// }
// soket.addEventListener("message", (message) => {
//   console.log(message.data); //hello world 메시지 받아줌.
// }); //ws는 단순히 이벤트를 주고받는것밖에 하지않음

// form.addEventListener("submit", sendMessage);
