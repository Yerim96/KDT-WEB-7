<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h4>방명록 입니다다다다~ 수상한 방명록~</h4>
    <form id="register">
        <fieldset>
            <legend>방명록 ^^</legend>
            <label for="name">name<input type="text" name="name" placeholder="이름을 입력하세요" ></label></br>
            <label for="comment">comment<input type="text" name="comment" placeholder="남길말" ></label></br>
        
            <button type="button" onclick="createVisitor()">등록</button>
        </fieldset>
        </form>
        <hr />


        <table border="1" cellspacing="0">
            <thead>
                <tr>
                  <th>id</th>
                  <th>작성자</th>
                  <th>방명록</th>
                  <th>수정</th>
                  <th>삭제</th>
                </tr>
              </thead>
              <tbody>
                <% for(let i = 0; i < test.length; i++) {%>
                <tr id="tr_<%= test[i].id %>">
                  <td>
                    <a href="/visitor/get?id=<%= test[i].id %>" target="_blank">
                      <%= test[i].id %>
                    </a>
                  </td>
                  
                  <td><%= test[i].name %></td>
                  <td><%= test[i].comment %></td>
                  <td>
                    <button type="button" onclick="editVisitor('<%= test[i].id %>')">
                      수정1
                    </button>
                  </td>
                  <!-- 몇번을 수정하는지 함수에 인자값을 넣어줘야함. -->
                  <td>
                    <button type="button" onclick="deleteVisitor('<%= test[i].id%>')">
                      삭제1
                    </button>
                  </td>
                </tr>
        
                <% } %>
        </table>
  </body>
</html>
